import { createContext } from "react";

// Create a new context for providing a global flash message component.
// This context can be used to display messages like success or error notifications,
export default createContext();
