import { createContext } from "react";

// Create a new context for providing global error state.
// This context can be used to set and access error state across the app.
export default createContext();
