import { createContext } from "react";

// Create a new context for providing a global user context
// This context can be used to display parks that a user has access to
export default createContext();
